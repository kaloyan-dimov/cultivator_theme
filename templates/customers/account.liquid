{% assign selected = "account" %}
{% include 'account_nav' with selected %}
<div class="section flex flex-col mt-[48px]">
    <div class="bg-white rounded-[20px] w-full flex flex-col p-6 border border-[#E6DFDB] m-auto max-w-[868px]">
      <div class="flex justify-between">
        <h2 class="body-I-semibold text-taupe-x">{{ customer.first_name }} {{ customer.last_name }}</h2>
        <h2 class="body-m-semibold text-taupe-x underline">Reset password</h2>
      </div>
      <h2 class="body-m text-taupe-x">{{ customer.email }}</h2>
      {% comment %}
        <h2>Password length</h2>
        <h2 class="recover_password_trigger">Reset password</h2>
      {% endcomment %}
    {% render "recover_password_form" %}
  </div> 
</div>