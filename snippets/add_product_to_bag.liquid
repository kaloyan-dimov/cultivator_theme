<div class="add_to_bag flex justify-center items-center rounded-[24px] bg-[#CCAC71] text-[#54443B] text-[14px] uppercase w-[calc(100%-32px)] h-[56px] absolute bottom-[16px] left-[16px]">
  {% if product.has_only_default_variant %}
    <h3 class="text-white">Add to Bag</h3>
  {% else %}
    <h3 class="text-white" href="/cart/add?id={{ variant.id }}&quantity=1 text-white add_product_variants">CHOOSE OPTIONS</h3>
    <div class="flex flex-col w-full absolute left-[0] bg-white  h-[114px] top-[-114px] pt-[21px] ">
      <div class="flex gap-[11px] justify-between">
        {% for variant in product.variants %}
          <h4
            data-id="{{ variant.id }}"
            data-price="{{ variant.price | money_without_trailing_zeros }}"
            class="
            flex flex-grow h-[34px] border border-[#E6DFDB] justify-center items-center rounded-[12px]
            {% if forloop.index == 1 %}
              active
            {% endif %}">
            {{ variant.option1 }}</h4>
        {% endfor %}
      </div>
      <h4 class="">Price: {{ product.first_available_variant.price | money_without_trailing_zeros }}</h4>
    </div>
  {% endif %}
</div>